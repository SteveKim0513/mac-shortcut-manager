# 원칙

이 문서의 원칙은 기능 추가·리팩터링과 무관하게 유지된다. 새 기능이 원칙과 충돌하면 기능을 포기하거나 원칙에 맞게 재설계한다 — 원칙을 깎아내지 않는다.

## 1. 스크립트가 곧 단축어다 (script-only)

- 모든 단축어는 `.sh` 파일 하나다. 별도 DB, JSON 설정 파일, 시각적 액션 빌더를 만들지 않는다.
- 메타데이터(이름/단축키/아이콘/카테고리/트리거)는 스크립트 상단의 `# @msm-*` 주석에만 존재한다. 스크립트 밖에 "진실의 원천"을 두지 않는다.
- 로직이 복잡해지면 쉘 스크립트를 더 잘 짜는 것으로 해결한다(함수, 파이프라인, 다른 CLI 도구 호출). 조건문·반복문을 표현하는 UI를 앱 안에 만들지 않는다.

## 2. 폴더가 곧 상태다

- `~/Documents/ShortcutScripts`의 `.sh` 파일 목록이 앱의 전체 상태다. 앱은 이 폴더를 감시(watch)하고 반영할 뿐, 별도로 상태를 소유하지 않는다.
- Finder에서 직접 추가/수정/삭제하든, git으로 버전 관리하든, 다른 기기와 폴더를 동기화하든 항상 정확히 반영되어야 한다.

## 3. 트리거는 늘어나도 실행 단위는 하나다

- "언제 실행할지"(단축키, 시간, 앱 실행, Wi-Fi 연결 등)는 계속 늘어날 수 있다. 하지만 "무엇을 실행할지"는 언제나 스크립트 하나뿐이다. 트리거 종류가 늘어난다고 실행 모델이 복잡해지면 안 된다.

## 4. 독립 앱이다 — macOS 단축어 앱과 통합하지 않는다

- Shortcuts.app은 폴더를 감시하는 구조가 아니고(SQLite 내부 DB, 바이너리 plist 내보내기), 서드파티가 목록을 직접 읽는 공식 API도 없다. 억지로 맞추면 오히려 복잡해진다.
- 필요하면 단방향 "내보내기"(스크립트를 감싼 `.shortcut` 파일 생성) 정도만 고려한다. 양방향 동기화·실시간 연동은 시도하지 않는다.

## 5. 단순함이 기능보다 우선한다

- macOS 단축어 앱의 모든 기능을 따라잡는 게 목표가 아니다. "쉘 스크립트를 아는 사람이 5분 안에 단축어를 만들 수 있다"가 목표다.
- 새 기능을 넣을 때 항상 묻는다: "이게 여전히 `.sh` 파일 하나 + 주석 몇 줄로 설명되는가?" 아니라면 이 앱의 기능이 아니다.
